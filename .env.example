# =============================================================================
# EverydayAI CLI Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# The CLI will automatically load these settings on startup
#
# Environment variable format for nested config:
#   SECTION__SETTING=value
# Example: YOUTUBE__COOKIES_BROWSER=safari sets youtube.cookies_browser

# =============================================================================
# API Configuration (REQUIRED)
# =============================================================================

# Your OpenAI API key (required for all AI operations)
# Get your key from: https://platform.openai.com/api-keys
API__OPENAI_API_KEY=

# Optional: Additional API keys for other AI services
# API__BROWSERUSE_API_KEY=your-browseruse-api-key
# API__ANTHROPIC_API_KEY=your-anthropic-api-key
# API__GOOGLE_API_KEY=your-google-api-key
# API__ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Optional: Custom OpenAI API base URL (for proxies or alternative endpoints)
# API__OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: API request timeout in seconds (default: 600)
# API__TIMEOUT_SECONDS=600

# Optional: Maximum retry attempts for failed API calls (default: 3)
# API__MAX_RETRIES=3

# ============================================================================
# OPTIONAL: YouTube Configuration
# ============================================================================
# Browser to extract cookies from: chrome, firefox, safari, edge
# YOUTUBE__COOKIES_BROWSER=safari

# Path to custom cookies file (Netscape format)
# YOUTUBE__COOKIES_FILE=~/.ei_cli/youtube_cookies.txt

# Max consecutive fragment failures before aborting (default: 10)
# YOUTUBE__MAX_FRAGMENT_FAILURES=10

# Number of retry attempts for downloads (default: 3)
# YOUTUBE__RETRY_ATTEMPTS=3

# Download timeout in seconds (default: 300)
# YOUTUBE__TIMEOUT_SECONDS=300

# ============================================================================
# OPTIONAL: Transcription Configuration
# ============================================================================
# Automatically chunk large audio files (default: true)
# TRANSCRIPTION__AUTO_CHUNK=true

# Maximum chunk size in MB (default: 20, API limit is 25MB with overhead)
# TRANSCRIPTION__MAX_CHUNK_SIZE_MB=20

# Target chunk duration in seconds (default: 600 = 10 minutes)
# TRANSCRIPTION__CHUNK_DURATION_SECONDS=600

# ISO-639-1 language code (e.g., 'en', 'es', 'fr')
# TRANSCRIPTION__LANGUAGE=en

# Keep intermediate chunk files (default: false)
# TRANSCRIPTION__SAVE_INTERMEDIATE=false

# ============================================================================
# OPTIONAL: Text-to-Speech Configuration
# ============================================================================
# Voice: alloy, echo, fable, onyx, nova, shimmer (default: nova)
# TTS__VOICE=nova

# Model: tts-1 (faster) or tts-1-hd (higher quality, default)
# TTS__MODEL=tts-1-hd

# Playback speed multiplier: 0.25 to 4.0 (default: 1.0)
# TTS__SPEED=1.0

# ============================================================================
# OPTIONAL: Workflow Configuration
# ============================================================================
# Directory for workflow outputs (default: ./workflow_outputs)
# WORKFLOW__OUTPUT_DIR=./workflow_outputs

# Save workflow state for resumability (default: true)
# WORKFLOW__SAVE_STATE=true

# Enable parallel execution of independent tasks (experimental, default: false)
# WORKFLOW__PARALLEL_EXECUTION=false

# Stop workflow on first error vs continue with warnings (default: true)
# WORKFLOW__FAIL_FAST=true

# ============================================================================
# NOTES
# ============================================================================
# - Use double underscore (__) for nested configuration
# - Environment variables override .env file values
# - See config.yaml.example for YAML configuration format
# - API key can also be set with: export EI_API_KEY=your-key
